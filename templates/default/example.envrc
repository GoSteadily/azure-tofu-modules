# EXPORTS

#
# https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs#subscription_id-1
#
# Use `az account list` to list all subscription IDs and details. Pick the one you need.
#
export ARM_SUBSCRIPTION_ID=

# FUNCTIONS

t () {
  tofu -chdir="$PROJECT_ROOT/root" "$@"
}

ts () {
  tofu -chdir="$PROJECT_ROOT/storage" "$@"
}

export -f t ts
